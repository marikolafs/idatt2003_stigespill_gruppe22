package edu.ntnu.idi.idatt.view;

import edu.ntnu.idi.idatt.controller.LadderGameController;
import edu.ntnu.idi.idatt.controller.WelcomeController;
import javafx.scene.Group;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.stage.Stage;

public class WelcomeView {

  WelcomeController welcomeController = new WelcomeController();

  /*
  public void showWelcome(Stage stage) throws Exception {

    Group root = new Group();
    Scene welcomeScene = new Scene(root);
    Scene ladderScene = new Scene(root);

    Button ladderButton = new Button("LadderGame");
    root.getChildren().add(ladderButton);

  }
   */

}
